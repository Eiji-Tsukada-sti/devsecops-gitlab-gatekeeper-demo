# ベースイメージとしてnginxの公式イメージを使用
FROM registry.gitlab.local.example.com/devsecops-user/devsecops-build-project/nginx:latest

# 静的ファイルをコンテナの指定されたパスにコピー
COPY src/html /usr/share/nginx/html

# nginxの設定ファイルをコンテナの指定されたパスにコピー（今回はデフォルト設定のため、COPYは不要）
COPY src/nginx.conf /etc/nginx/nginx.conf

# コンテナがListenするポートを定義
EXPOSE 8080